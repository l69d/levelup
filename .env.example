# Optional. Users bring their own key via the in-app dialog (stored in their
# browser's localStorage, sent only when they submit). Set this only if you
# want a server-wide fallback key for local dev / your own deployment.
ANTHROPIC_API_KEY=
