# All keys are OPTIONAL — visitors bring their own via the in-app dialog
# (stored in their browser's localStorage, sent only when they submit).
#
# Set these only if you want a server-wide fallback so visitors don't need
# to bring their own. User-supplied keys always take precedence.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
